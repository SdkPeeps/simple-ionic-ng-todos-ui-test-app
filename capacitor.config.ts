import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Todos-Lib-Test-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
